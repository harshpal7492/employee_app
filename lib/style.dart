import 'package:flutter/material.dart';

class Style extends StatelessWidget {
  final style30W = const TextStyle(fontSize: 30, color: Colors.white);

  final sizeBox200 = const SizedBox(
    height: 200,
    width: double.infinity,
  );
  final sizeBox50 = const SizedBox(
    height: 50,
    width: double.infinity,
  );
  final sizeBox30 = const SizedBox(
    height: 30,
    width: double.infinity,
  );
  final sizeBox10 = const SizedBox(
    height: 10,
    width: double.infinity,
  );

  const Style({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
